# TaskTracker
**The TaskTracker is a CLI(command line interface) -using a positional argument. this project unwinds the File Systems, Error handling, handling user inputs, JSON parsing,ArrayLists,CRUD operations**
**Concepts used Like:**
- OOPS
- args[0]--> here indicates the the first argument like [ java ProjectName **add**] here add is the first argument and args.length is 0 for now
- args[1]--> the next argument [ java projectName delete **1**] and args[2]--> next argument like [java projectName update 1 "**Eating**"]
- Integer.parseInt(args[1]) the command line arguments are array of strings
<---File Systems--->
- BufferedReader-->Read the every line in file
- regex -- //escape 
<--performing CRUD operations-->
- add,update,delete,list, mark

**so this is only readonly terminal where each command with positional arguments will display the output**
**we will read the json file and write back the changes to json file**
